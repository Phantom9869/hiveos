"""Code shared by the Router and Agent Runner Lambdas.

Both functions are packaged from CodeUri `backend/`, so `shared` is importable
as a top-level package from either handler. No Lambda layer needed.
"""
